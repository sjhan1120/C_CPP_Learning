#include <iostream>

int main() {
    int min, x, y, max, j, k;
    min = 1;
    x = 2;
    y = 3;
    max = 4;
    j = 0b1;
    k = 0b10101;

    bool i = true;

    bool in_bound = min <= x && x <= y && y <= max;
    std::cout << in_bound << std::endl;

    std::cout << not i << std::endl;

    k << j;

    std::cout << k;
    
    return 0;
}